/**
 * Provides five servlet classes which are used for different contexts of the program: calculation context, check parameter context and three with history of calculations context (two of them use database). Also provides one class which is used to hold connection with database.
 */

package controler;
