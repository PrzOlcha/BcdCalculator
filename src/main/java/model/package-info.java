/**
 * Provides two classes, one fo them check paramaterers and the other perform calculations and convert numbers. 
 * Also provides one exception class, which is throw when calculations is started using incorrect quantity of parameters.
 */

package model;
